import React from 'react'

function Heading({variant}) {
    const classes = "";
    const defaultClasses = "text dark:text";
  return (
    <span className={classes}>Heading</span>
  )
}

export default Heading